// Placeholder de desarrollo. En build release, el hook before_compile
// (hooks/inject-build-config.js) sobrescribe este fichero en platforms/
// con window.APP_DEBUG = false. Aquí queda en true para no servir
// impresiones/clics reales de AdMob mientras se trabaja en local.
window.APP_DEBUG = true;
